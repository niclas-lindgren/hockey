# Verification Report

STATUS: PASS

| Criterion | Verdict | Evidence |
| --- | --- | --- |
| `requirements.lock` contains exact resolved versions and hashes. | PASS | `requirements.lock` starts with the refresh-script header, contains `==` pinned packages generated by `pip-compile --all-extras --allow-unsafe --generate-hashes`, and includes `--hash=sha256:` entries; covered by `tests/test_dependency_lock_workflow.py::test_committed_lock_has_exact_versions_hashes_and_expected_groups`. |
| `grep -R "requirements.lock" .github/workflows/ci.yml scripts/install.sh scripts/package-desktop-backend.sh scripts/package-desktop-backend.ps1 docs/ci.md README.md` shows CI, install, packaging, and docs use the locked dependency set. | PASS | The grep command was run successfully and showed `requirements.lock` references in CI, install, packaging scripts, docs, and README. |
| `scripts/check dependency-lock` passes. | PASS | `scripts/check dependency-lock` completed with `✅ Canonical checks passed`. |
| `python3 -m pytest --no-cov -v tests/test_dependency_lock_workflow.py` passes. | PASS | 6 tests passed. |
| `python3 -m pytest --no-cov -q tests/test_dependency_lock_workflow.py tests/test_makefile_operator_interface.py` passes. | PASS | 19 tests passed. |
| Standard quality gate passes. | PASS | `pi_next_quality_gate(level="standard")` ran `python3 -m pytest -q -m "not slow and not integration"`: 1190 passed, 1 skipped, 26 deselected. |
