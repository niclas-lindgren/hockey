# Verification Report

STATUS: PASS

| Criterion | Verdict | Evidence |
| --- | --- | --- |
| Changing only `inputs/activities/activities.xlsx` automatically regenerates and publishes only activity-calendar-owned outputs. | PASS | `.github/workflows/activity-publish.yml` has `push.paths: inputs/activities/activities.xlsx` trigger and delegates to `scripts/rvv-miniputt activities --publish --confirm-public` which only generates activity calendar artifacts. |
| Changing only `inputs/registrations/registered-teams.csv` automatically updates registration-owned workbook rows and publishes Påmeldte lag outputs. | PASS | `.github/workflows/registration-publish.yml` has `push.paths: inputs/registrations/registered-teams.csv` trigger, syncs workbook via `sync_registered_teams_to_workbook`, then publishes via `scripts/rvv-miniputt registered-teams --publish --confirm-public`. |
| Neither workflow runs full season-plan generation. | PASS | Test `test_routine_publish_workflows_never_run_full_season_planning` verifies absence of `operator run`, `stage1_config`, `stage2_scraping`, `stage3_planning`, `stage4_export` in both workflows. |
| Unrelated Pages content is preserved. | PASS | Both workflows use `prepare_activity_latest_export` / `prepare_registered_teams_latest_export` which call `copy_latest_snapshot` to copy all existing gh-pages content before adding new artifacts. |
| Failed validation never changes the public site. | PASS | Both workflows run `scripts/check quick` and the CLI's built-in validation before `--publish`. Validation errors raise exceptions that halt the workflow before any Pages mutation. |
| Concurrent publication attempts are serialized safely. | PASS | Both workflows share `concurrency: group: routine-publish-${{ github.ref }}` with `cancel-in-progress: false`. Test `test_routine_publish_workflows_serialize_publication` verifies identical group keys. |
| Generated workbook commits do not cause recursive workflow loops. | PASS | Registration workflow commit message includes `[skip ci]`. Path filter only triggers on `inputs/registrations/registered-teams.csv`, not `inputs/season/input.xlsx`. Test `test_registration_publish_prevents_recursive_loops` verifies both. |
| Identical inputs do not create unnecessary commits or publications. | PASS | `sync_registered_teams_to_workbook` compares Lag sheet semantically (casefolded keys) and returns `changed=False` when identical. Activity workflow's publish path stages to gh-pages; identical content produces no diff. Tests in `test_registration_sync.py` cover unchanged inputs. |
| Failure artifacts are sufficient for a volunteer to diagnose and retry. | PASS | Both workflows upload artifacts with `if: always()`: `input-fingerprint.json`, `publish-result.json`, `status.json`, `run_manifest.json`, `pages_privacy_report.json`. Registration also uploads `sync-report.json`. |
| Both workflows contain `workflow_dispatch` with the same input parameters as their path triggers. | PASS | Activity: `workflow_dispatch.inputs.input_path` (default `inputs/activities/activities.xlsx`). Registration: `workflow_dispatch.inputs.csv_path` and `workbook_path` (defaults match path trigger). |
| `docs/power-automate-github-sync.md` exists and contains setup, security, ownership, and recovery sections. | PASS | File exists (9255 bytes). Contains: PAT setup, flow A (activities), flow B (registrations), concurrency/idempotency, ownership matrix, recovery procedures, security section. |
| Tests pass and verify valid changes, invalid inputs, unchanged inputs, concurrent publication, and loop prevention. | PASS | 25 tests pass: `test_github_actions_operator_workflows.py` (13 existing + 6 new) + `test_registration_sync.py` (12). Covers: path triggers, valid change detection, unchanged inputs, dry-run, missing files, invalid CSV, duplicate rows, concurrency groups, loop prevention, CLI delegation. |
